#!/bin/bash

# Name: Encrypt File (Vault)
# MimeType: all/all
#
# Encrypt a file into the GPG vault.

BIN="$(command -v vault)" || { echo "vault not found in PATH" >&2; exit 1; }

if [[ $# -eq 0 ]]; then
    echo "Usage: encrypt-file <file>" >&2
    exit 1
fi

exec "$BIN" add "$@"
