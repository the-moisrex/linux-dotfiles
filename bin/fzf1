#!/bin/bash

if ! command -v fzf >/dev/null; then
    echo "fzf is not installed." >&2
    exit 1
fi

if [ "$1" == "--help" ]; then
    echo "shortcut for 'fzf -f QUERY | head -1'"
    exit
fi

fzf -f "$@" | head -1