#!/bin/bash

if [[ "$1" == "--help" || "$1" == "-h" ]]; then
    echo "Usage: command | strip-colors"
    echo
    echo "Remove ANSI color escape sequences from stdin."
    exit 0
fi

cat | sed -r "s/\x1B\[([0-9]{1,3}(;[0-9]{1,3})*)?[mGK]//g"
